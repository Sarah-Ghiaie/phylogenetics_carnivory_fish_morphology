#!/bin/bash

# project_setup.sh
# Creates the project directory structure

mkdir -p data/raw/{seq_raw,traits_raw,metadata} \data/clean/{seq_align,trees,clean_traits} \results/{figures,reports} \logs scripts
